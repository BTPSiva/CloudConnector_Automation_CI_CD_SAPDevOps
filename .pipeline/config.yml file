general:
  terraformCredentialsId: "terraform-service-account"  # Jenkins credentials ID for cloud provider auth
  terraformDir: "./terraform"                          # Path where main.tf is located

stages:
  initStage:
    enabled: true
  validateStage:
    enabled: true
  planStage:
    enabled: true
  applyStage:
    enabled: false    # Enable only for prod or controlled pipelines

steps:
  terraformInit:
   

  terraformValidate:
   

  terraformPlan:
   
  terraformApply:
    autoApprove: true                   # Automatically approve plan (use with caution)
